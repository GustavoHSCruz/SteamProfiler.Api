# For a Cloudflare Tunnel connector, trust only the connector's source address
# as seen by this nginx container. Do not trust all addresses or the whole LAN.
# Copy to a .conf file in this directory and replace the example address.
# set_real_ip_from 192.0.2.10;
# real_ip_header CF-Connecting-IP;
